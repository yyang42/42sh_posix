lel ()
{
	case $(abc def)`ijk lmn`$((zyx)) in
    pouet|pouf) echo abc; (command lel 2> pouf); a=666;;
    [7-8]*) ! JAMBON > PUTE;;
    ?*?*?*?|?????) En vrai; je dois confesser quelque chose.
        echo case 9x
        ;;
	99) DEpuis le début. On triche \: '( laaaaaaal )'
        ;;
    *)
		for i in $(seq 1 1000) ; do echo aler a +++; done &
        ;;
    esac 2>&1 & lol
	lel
}

command -V lel
